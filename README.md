# StudyPlan MVP
Simple MVP where students upload syllabus + exam date → tailored study plan.

## Setup
1. Clone repo
2. Copy `.env.example` → `.env` and fill keys
3. Run `npm install` (or `pip install -r requirements.txt`)
4. Start with `npm run dev` (or `python main.py`)
